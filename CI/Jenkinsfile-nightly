pipeline {
    agent any

    stages {

        stage('Docker') {
            steps {
                sh 'docker build -f Dockerfile-playwright -t my-playwright .'
                sh 'docker build -f Dockerfile-aws-cli -t my-aws-cli .'
            }
        }
    }
}